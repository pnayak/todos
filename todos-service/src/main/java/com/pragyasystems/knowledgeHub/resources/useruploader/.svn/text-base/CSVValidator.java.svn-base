package csvuploader;

public class CSVValidator {

	
	
	public static String getError(String[] userProp, int lineNo) {
		
		if (userProp.length != 5 ){
			return "Line No. " + lineNo + " : Incorrect No. of fields in CSV line"; 
		} 
		
		return null;
	}

}
